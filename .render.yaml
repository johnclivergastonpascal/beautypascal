services:
  - type: web
    name: go-api
    env: go
    rootDir: ./api           # 📁 ruta donde está tu main.go
    buildCommand: go build -o app .
    startCommand: ./app
    plan: free
    autoDeploy: true
    envVars:
      - key: PORT
        value: 8080
